{
  "name": "poneglyph-api-worker",
  "version": "1.0.0",
  "private": true,
  "description": "API REST Cloudflare Worker — TANUKI-PONEGLYPH (One Piece TCG FR)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0",
    "@cloudflare/workers-types": "^4.20260701.0"
  }
}
